# Production Environment Configuration
# Bu dosyayı .env.production olarak kopyalayın ve gerçek değerlerle güncelleyin

# Sunucu Konfigürasyonu
NODE_ENV=production
PORT=5001

# PostgreSQL (Production)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=koop_production
DB_USER=koop_user
DB_PASSWORD=strong_password_change_this

# JWT (GÜVENLİ BİR SECRET KEY KULLANIN!)
JWT_SECRET=super-secure-production-jwt-key-change-this-123456789
JWT_EXPIRE=24h

# CORS (Domain adresiniz)
CORS_ORIGIN=https://yourdomain.com

# Mail Konfigürasyonu (Gmail örneği)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=bilgi@renkliorman.k12.tr
MAIL_PASS=gmail_app_password_change_this
MAIL_FROM_NAME=Eğitim Kooperatifi

# Güvenlik (Production'da değiştirin)
# JWT_SECRET: Minimum 32 karakter, karmaşık
# DB_PASSWORD: Güçlü PostgreSQL şifresi  
# MAIL_PASS: Gmail App Password (16 haneli)
